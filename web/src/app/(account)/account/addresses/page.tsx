import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Mes adresses' }
export default function AddressesPage() {
  return <div><h1 className="text-2xl font-bold">Mes adresses</h1></div>
}
